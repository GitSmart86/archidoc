[package]
name = "archidoc-types"
version = "0.1.0"
edition = "2021"
description = "Shared types for the archidoc toolchain"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
