/**
 * @c4 container
 *
 * WebSocket service — manages real-time connections.
 *
 * | File | Pattern | Purpose | Health |
 * |------|---------|---------|--------|
 * | `connection.ts` | -- | Connection lifecycle management | active |
 * | `protocol.ts` | Strategy | Message encoding strategies | stable |
 */
