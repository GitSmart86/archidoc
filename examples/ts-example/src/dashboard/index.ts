/**
 * @c4 container
 *
 * Dashboard UI — real-time data visualization and user interaction.
 *
 * @c4 uses api "Fetches data" "REST/HTTP"
 * @c4 uses websocket "Live updates" "WebSocket"
 *
 * | File | Pattern | Purpose | Health |
 * |------|---------|---------|--------|
 * | `charts.ts` | Observer | Chart rendering with live data | active |
 * | `state.ts` | Facade | Application state management | stable |
 */
